
package com.example.demo;

import static org.assertj.core.api.Assertions.assertThat;

import java.net.MalformedURLException;
import java.util.List;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;



import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;


//@SpringBootTest
class DemoApplicationTests {

	/*
	 * @Autowired private IAccount iAccount;
	 * 
	 * @MockBean private AccountController accountController;
	 * 
	 * @Autowired private IClient repository;
	 * 
	 * private static Logger LOGGER = null;
	 * 
	 * @BeforeClass public static void setLogger() throws MalformedURLException {
	 * System.setProperty("log4j.configurationFile", "log4j2-testConfig.xml");
	 * LOGGER = LogManager.getLogger(); }
	 * 
	 * // @Test // public void testFindAll() { // List<Account> items =
	 * iAccount.findAll(); // // } // // @Test // public void getSaveNewProduct()
	 * throws Exception { // Client product = repository.findByCustomerId("000001");
	 * // // System.out.println(product.getCustomerName()); // //
	 * assertThat(product.getCustomerName().equals("01099")); // }
	 */
}
